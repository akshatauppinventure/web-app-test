# Host sshd log (public, key-only SSH since ADR-0026; decisions apply at the HTTP layer only). Mounted read-only by the edge stack (T16);
# absent in the local test, which CrowdSec tolerates with a warning.
source: file
filenames:
  - /var/log/host/auth.log
labels:
  type: syslog
