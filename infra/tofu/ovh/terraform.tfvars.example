# Copy to terraform.tfvars (gitignored; encrypt with sops to terraform.tfvars.sops when committing, see README).
# Same keys as infra/tofu/upcloud/terraform.tfvars.example: the file is interchangeable between providers.
admin_ssh_public_key = "ssh-ed25519 AAAA... owner@laptop"
cloud_init_edge_path = "../../../.tofu-rendered/edge.yaml"
cloud_init_core_path = "../../../.tofu-rendered/core.yaml"
# zone                    = "US-EAST-VA-1"
# template_name           = "^Ubuntu 26\\.04"
# edge_plan               = "d2-4"
# core_plan               = "d2-8"
# private_network_vlan_id = 42
