# Docker starts after nftables (policy DROP first) and after the network is up, so ports published on
# the private-network address (10.0.0.x) bind at boot (ADR-0014, ADR-0026).
[Unit]
After=nftables.service network-online.target
Wants=network-online.target
