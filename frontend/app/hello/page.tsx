// Placeholder: T07 turns this into a server component that calls GET /v1/hello with the
// caller's access token and renders the visit message.
export default function HelloPage() {
  return (
    <>
      <h1>Hello</h1>
      <p>Sign-in and the API call are wired up in task T07.</p>
    </>
  );
}
